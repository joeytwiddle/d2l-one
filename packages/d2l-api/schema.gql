scalar UserID
scalar RescueID
scalar SiteID
scalar Date

schema {
  query: Query
  mutation: Mutation
}

type Query {
  me: User!
  user(id: UserID!): User
  allUsers: [User]
  rescues: [Rescue]
  #search(term: String!): [SearchResult!]!
}

enum Role {
  USER
  ADMIN
}

type User {
  id: UserID!
  name: String!
  telegramName: String!
  email: String
  role: Role!
}

type Rescue {
  id: RescueID!
  date: Date!
  site: Site!
  rescuers: [User]!
}

type Site {
  id: SiteID!
  name: String!
  geoLocation: String!
}

query GetUser {
  me {
    id
    name
  }
}

query GetAllRescues {
  rescues {
    id
    date
    site {
      id
      name
    }
    rescuers {
      id
      name
    }
  }
}

type Mutation {
  assignSelfToRescue(rescueId: RescueID!): Rescue!
}
